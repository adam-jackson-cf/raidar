# Benchmark View

Repo-native benchmark dashboard over `experiments/benchmarks` artifacts.

## Commands
- `npm run build-data`
- `npm run serve`

Use repo-root make targets instead:
- `make benchmark-view-build`
- `make benchmark-view-serve`
