# Task

Improve `src/homepage.ts` so the generated homepage is benchmark-competitive and reaches or exceeds a score of **88.05643109423825** while keeping all verification gates green.

## Required outcomes
- Keep semantic landmarks (`header`, `main`, `footer`).
- Keep section ids: `hero`, `features`, `proof`, `cta`.
- Keep one primary CTA marker: `data-testid="primary-cta"`.
- Upgrade visual/content quality and clarity with concrete user-centric copy.
- Do not introduce placeholder text or TODO markers.

## Verification
Run:
- `bun run lint`
- `bun run test`
- `bun run verify`
