{
  "name": "homepage-iteration-starter",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "bunx tsc --noEmit",
    "test": "bun test --bail",
    "verify": "bun run lint && bun run test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
