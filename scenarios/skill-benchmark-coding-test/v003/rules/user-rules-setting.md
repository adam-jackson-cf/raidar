# Scenario Rules

- Do not add TODO placeholders.
- Keep the public API exactly `sumEven(nums: number[]): number`.
- Use a direct reduce implementation and six small deterministic tests only.
- Apply the formatter's exact suggested output if lint fails.
