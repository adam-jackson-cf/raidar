# Scenario Rules

- Do not add TODO placeholders.
- Keep the public API exactly `sumEven(nums: number[]): number`.
- Use the existing Bun/Vitest/TypeScript project setup.
- Treat `bun run lint` as a required completion gate; fix formatter and import-order findings before finishing.
