Build a small TypeScript utility and tests.

Requirements:
- Implement `src/lib/math.ts` with function `sumEven(nums: number[]): number`.
- It must return the sum of even integers only.
- Ignore values that are not integers.
- Add unit tests in `src/test/math.test.ts`.
- Keep the existing project scripts working.
